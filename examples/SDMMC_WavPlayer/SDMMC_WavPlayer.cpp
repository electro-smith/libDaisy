/** Basic audio file playback of WAV player
 *  
 * 
 *  Uses one button to trigger file playback, or restart the file while running.
 */
#include "daisy_seed.h"
#include "Stuff.h"

using namespace daisy;

DaisySeed hw;

int main(void)
{
    hw.Init();
    while(1) {}
}
